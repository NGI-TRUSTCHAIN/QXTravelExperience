export enum AuthCardFormTypeEnum {
    login = 'login',
    register = 'register',
}

export enum TabTypeEnum {
    all = "all",
    active = "active",
    suspend = "suspend",
}

export enum StatusEnum {
    all = "all",
    pending = "pending",
    denied = "denied",
    approved = "approved",
}